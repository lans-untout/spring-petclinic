apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    uid: prometheus          # fixed UID – we'll reference it in the dashboard
    isDefault: true